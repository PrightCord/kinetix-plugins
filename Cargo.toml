# Standalone workspace for the Kinetix plugin SDK and first-party plugins.
[workspace]
members = ["sdk", "plugins/antigravity-oauth", "plugins/claude-code-oauth"]
resolver = "2"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true
